/**
 * @super-app/tailwind-config — 共享 Tailwind CSS 配置（待启用）
 *
 * L0 前端构建配置层。当前各 app 使用内联配置；待跨 app 设计系统统一后，
 * 此包将提供共享的 theme/preset/content 配置入口。
 *
 * 设计约束：零 @super-app/* 依赖（L0 基础设施）。
 */
export {}
